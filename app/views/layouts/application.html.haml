!!!
%html{:lang => I18n.locale.to_s}
  %head
    %meta{:charset => 'utf-8'}

    %title
      = "Gr"
      
    = stylesheet_link_tag :all
    = stylesheet_link_tag "vendor/Aristo/jquery-ui.css"
    / todo: move to assets.yml like in diaspora
    = javascript_include_tag "vendor/jquery.js"
    = javascript_include_tag "vendor/jquery-ui"
    / Overrides default locale of datepicker only if the localization file is found!
    = javascript_include_tag "vendor/i18n/jquery.ui.datepicker-#{I18n.locale.to_s}"
    = javascript_include_tag "vendor/jquery.validate"
    = javascript_include_tag :defaults
    = csrf_meta_tag 

  %body
    #container
      - flash.each do |name, msg| 
        %div{ :id => "flash_#{name}" }
          %div
            %p= msg

      - if user_signed_in?
        = link_to 'Kirjaudu ulos', destroy_user_session_path
      - else
        = link_to 'Kirjaudu sisään', new_user_session_path
      |
      = link_to "Tuotteet", products_path
      |
      = link_to "Reseptit", recipes_path
      |
      = link_to "Suosikit", favorites_path
      |
      = link_to "Syöty", eaten_path
    
      <p>Huom! Käyttäkää desimaalierottimena pistettä eikä pilkkua!</p>
    
      = yield
