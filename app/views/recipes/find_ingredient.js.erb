$("#add_ingredient_dialog").html("<%= escape_javascript(render("ingredient")) %>").dialog({
	modal:true,
	buttons: {
		"Lisää": function () {
			alert($(this).dialog.html);
			$("#ingredients").append("<p>foo</p>")
		},
		Cancel: function() {
			$(this).dialog("close");
		}
	}
	});

