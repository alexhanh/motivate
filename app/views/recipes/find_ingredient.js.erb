$("#add_ingredient_dialog").html("<%= escape_javascript(render("ingredient")) %>").dialog({
	modal:true,
	show:'fade',
	hide:'fade',
	buttons: {
		"Lisää": function () {
			$(this).dialog("close")
			$("#new_ingredient").submit();
		},
		Cancel: function() {
			$(this).dialog("close");
		}
	}
	});

