$("#add_ingredient_dialog").html("<%= escape_javascript(render("ingredient")) %>").dialog({
	modal:true,
	buttons: {
		"Lisää": function () {
			$("#ingredients").append($("#add_ingredient_form").html());
		},
		Cancel: function() {
			$(this).dialog("close");
		}
	}
	});

